<h1><%= title %></h1>

<p><a href="/packages">&lt;&lt; back to package list</a></p>
<br />
<p><b>You need to restart the server to apply the changes !</b></p>

<pre>
<% for(var i = 0 ; i < npmlog.length ; i++) { 
      var log = npmlog[i];
      if(log.level === 0) { %>
npm info <%= log.pref %> <%= log.msg %>
<% } } %>
</pre>


<p><a href="/packages">&lt;&lt; back to package list</a></p>